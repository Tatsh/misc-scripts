#!/usr/bin/env bash
# This file is intentionally  set without +x permissions.
while IFS=$'\n' read -r name; do
    pylint "$name"
done < <(file -i ./* archived/* | grep -F text/x-script.python | cut -d: -f1 | sort -u)
