#!/usr/bin/env python
import sys

if __name__ == '__main__':
    # argv mode
    if len(sys.argv) >= 2:
        for arg in sys.argv[1:]:
            print(arg.strip())
        sys.exit()
    # stdin mode
    for arg in sys.stdin.readlines():
        print(arg.strip())
